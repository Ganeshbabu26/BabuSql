USE BABU;

CREATE TABLE SALARY (
    SID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    ID INT NOT NULL,
    SDATE DATE NOT NULL,
    AMT INT NOT NULL
);

INSERT INTO SALARY (ID, SDATE, AMT)
VALUES
(1,'2018-09-01',10000),
(2,'2018-09-01',7500),
(3,'2018-09-01',6000),
(4,'2018-09-01',4000);
SELECT * FROM SALARY;

SELECT 
EMP.NAME, EMP.DESIGN, 
SALARY.SDATE, SALARY.AMT FROM 
EMP INNER JOIN SALARY
ON EMP.ID = SALARY.ID;

SELECT 
EMP.NAME, EMP.DESIGN, 
SALARY.SDATE, SALARY.AMT FROM 
EMP LEFT JOIN SALARY
ON EMP.ID = SALARY.ID;